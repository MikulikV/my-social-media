let initialState = {
    friends: [
        { id: 1, name: "Vadim" },
        { id: 2, name: "Dinara" },
        { id: 3, name: "Violetta" }
      ],
};

const friendsReducer = (state = initialState, action) => {
    return state;
};
  
export default friendsReducer;
  